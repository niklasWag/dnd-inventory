// Build-time globals injected by Vite's `define` (see vite.config.ts +
// vitest.config.ts). Both configs stamp the same value from
// `apps/web/package.json#version`.

declare const __APP_VERSION__: string;
