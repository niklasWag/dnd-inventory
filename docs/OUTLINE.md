# D&D 5e (2024) Inventory Management — Project Outline

A web application for managing inventories in **D&D 5e (2024 rules)**.
Designed as a **party manager** with full DM tooling. **Every user is always inside at least one party** — "solo" is just a party-of-one with the same data model and feature set.

> **Scope note:** Intended for **private use**. Content is based on the **2024 Player's Handbook** and **Dungeon Master's Guide**, with a **homebrew/custom items** module. Not for distribution.

---

## 1. Goals & Non-Goals

### Goals
- Track items, currency, encumbrance, and attunement for one or more characters.
- Support a **shared party stash** and a DM-controlled **recovered-loot pile**.
- Allow item transfers between characters and the party stash, with **per-item history** visible to owner + DM.
- Provide a **DM view** for handing out loot (per-hoard choice: shared pool or direct assign), running shops (static catalog, manual purchases), and managing magic-item identification.
- Allow **custom/homebrew items** to coexist with PHB/DMG content.
- Support **solo play** (party-of-one) using the same model as a multi-member party.
- Run **fully local** (browser localStorage) **or** against a **self-hosted Linux backend** for live party sync and backups.

### Non-Goals (v1)
- Full character sheet management (HP, spells, abilities) — **inventory-only minimum** character data.
- Combat tracking / initiative.
- VTT integration (maps, tokens).
- Public/multi-tenant SaaS hardening (private use).
- Full economy (regional pricing, haggle rolls) — base price + shop modifiers only.

---

## 2. Personas & Modes

| Persona | Primary needs |
|---|---|
| **Player** | View own Inventory, equip/unequip, manage attunement, see encumbrance, request transfers, claim loot, manage personal Storage stashes (chests, vaults). |
| **Banker** (optional player role) | Same as Player, plus authority over the Party Stash AND Recovered Loot: distribute currency and items to specific players. |
| **DM** | Read-all + write-via-actions in their party; hand out loot, run shops, identify magic items, appoint/revoke Banker. |
| **Solo user** | A party-of-one. Has all Player+DM rights over their own data. Can promote into a real party by inviting others. |

**Modes**
- **Local mode** — single-user, browser localStorage only. No Discord login. Everything is a party-of-one under the hood.
- **Self-hosted mode** — Discord OAuth login, server sync, backups. Solo data ("party-of-one") is also synced so it works cross-device. Party features (multi-member) require online; solo data continues to work offline and syncs on reconnect.

---

## 3. Core Features

### 3.0 Core Model: Everyone Is In a Party-of-N
- **Every user is always inside at least one party.** "Solo" is just a party-of-one — same data model, same features, no parallel solo path.
- A user can be in multiple parties simultaneously (DM of one, player in another, solo in a third).
- The full party feature set (Party Stash, Recovered Loot, shops, hoards) is available even in a party-of-one — useful when a player wants to manage everyone's loot themselves while the rest of the table doesn't use the app.

### 3.1 Identity & Entry Flow
- **Discord OAuth** is the only login method. No local accounts. No DM-bootstrap admin role.
- After login, the user lands on a **hub** with three actions:
  1. **Create a party** — user becomes that party's DM. Adds a player slot for themselves (so a single user is both DM and player by default; can opt out of having a character).
  2. **Join a party** — paste an invite code/link generated by another DM.
  3. **Create a solo (party-of-one)** — shortcut for "Create a party where I'm the only member"; user is DM + sole player.
- Roles are **per-party** (party creator = DM; joiners = players). A user can be a DM in one party and a player in another.
- **Permissive-until-others-join**: in a party with only one member, the DM/sole-player gets permissive defaults (e.g., can freely create custom items). As soon as a second user joins, standard party rules apply (DM-only custom items, etc.).

### 3.2 Party Management
- Party creator gets an **invite code/link**; revocable and regeneratable by the DM.
- Players join by pasting the code.
- **Leaving a party**: the player's items and currency are moved into the party's **Recovered Loot** stash. **If a Banker is appointed**, only the Banker can redistribute from it; **otherwise**, any party member can claim freely.
- DM can kick a player (same Recovered Loot behavior).

### 3.3 Characters & Stashes
Each character has:
- **Inventory** — exactly one carried stash, auto-created with the character. **Encumbrance applies here**; equip/attune state lives on items in this stash.
- **Storage** stashes — zero or more named non-carried stashes (e.g., "Chest at home", "Vault in Waterdeep", "Wagon"). **Encumbrance does NOT apply** to Storage. The user can transfer items freely between Inventory and Storage.

Character data stored (inventory-only minimum):
- name, species, class, level
- **STR** (used by encumbrance)
- **max attunement slots** — defaults to 3, **DM-overridable per character**
- encumbrance enforcement: **off / advisory / hard** (per-character; DM-controlled when 2+ members)

No HP, spells, AC, proficiencies in v1.

### 3.4 Inventory Operations
- Add item (from catalog or custom).
- Remove / consume / destroy item.
- **Equip / unequip** (armor, weapons, shields) — only meaningful on items in a character's Inventory stash.
- **Attune / unattune** — tracked with warnings; DM can raise the slot cap per character.
- Adjust quantity; identical items **auto-stack** (same definition + same notes).
- **Move item** between any two stashes the actor has permission for: Inventory ↔ Storage, Inventory ↔ Party Stash, character ↔ character, ↔ Recovered Loot.
- Bulk multi-select for move/delete.
- Notes per item instance (e.g., "given by Volo").
- **Per-item history** — owner and DM see who held the item and when it changed hands.

### 3.5 Currency & Treasure
- Track CP, SP, EP, GP, PP **on every stash** — Inventory, Storage, Party Stash, Recovered Loot — uniformly.
- Currency conversion helper.
- **Treasure hoard generator** (2024 DMG tables by CR/level band).
- "Split evenly" for Party Stash gold (Banker action; falls back to DM when no Banker).
- **Bulk currency entry** (post-MVP). MVP M4 ships ±1 inline +/− controls plus a Convert modal — sufficient for tweaks but painful for hoard drops ("loot +300 sp"). R7 iteration (see roadmap.md): editable cells that accept signed integers (`+300`, `-50`) and absolute targets (`=42`), each dispatching a single `currency-change` carrying the computed diff. Schema-unchanged.
- **Per-party economy controls** (post-MVP, opt-in). Two orthogonal knobs in Party Settings (§5.15) describe a campaign's economy:
  - **`Party.priceModifier: number`** (default `1.0`) — multiplies every PHB/DMG seed price. Covers the common silver-standard variant (`0.1` — a PHB 5 gp item becomes 5 sp at the table) and any homebrew economy the DM wants (`0.5` for "everything is half off", `2.0` for high-magic inflation, `0.25` for grim resource scarcity). Conceptually the party-scope mirror of `Shop.priceModifier` from §3.9 (the two compose: `finalCp = phbCostCp × party.priceModifier × shop.priceModifier`).
  - **`Party.baseCurrency: "cp" | "sp" | "ep" | "gp" | "pp"`** (default `"gp"`) — the **display ceiling** for canonicalized prices. The canonicalizer (below) never rolls a price *above* this denomination, so a gold-standard campaign reads "200 gp" rather than "20 pp", and a silver-standard campaign reads "200 sp" rather than "2 gp". Purely a display rule; storage and arithmetic always use real CP.
  - **PHB / DMG seed costs are stored verbatim** (`cost.amount = 5, cost.currency = "gp"` for a 5 gp item) and never rewritten. The seed is the canonical 2024 ruleset.
  - **The coin ladder is also never re-keyed**: `cp=1, sp=10, ep=50, gp=100, pp=1000` are physical coin denominations, not a pricing model. `CurrencyHolding` and `currency-change` always mean exactly what they say in physical coins regardless of either knob.
  - **The modifier applies only at the seed-price interpretation boundary**: when a catalog row is rendered and when a `purchase` / `sale` action is dispatched. `effectiveCostCp = round(phbCostCp × priceModifier)`. A 5 gp PHB item (= 500 cp) with `priceModifier: 0.1` becomes 50 cp at display + checkout; the party's `CurrencyHolding` pays in real coins.
  - **Display canonicalization rule (lives in `packages/rules/pricing.ts:formatPrice`).** Given an amount in CP and a `baseCurrency` ceiling, render in the **largest coin denomination ≤ `baseCurrency` where the value is a whole number**. Two intents working together:
    1. **Prefer whole numbers over fractions** — never render "0.5 gp" or "0.1 sp". A scaled-down 5 gp price (`500 cp × 0.1 = 50 cp`) under `baseCurrency: "gp"` renders as **"5 sp"** (50 cp divides cleanly by 10), never "0.5 gp". A `5 cp` price renders as **"5 cp"**, never "0.5 sp".
    2. **Don't roll up past the base currency** — never render "20 pp" under a gold-standard campaign. A `20 000 cp` PHB price under `baseCurrency: "gp"` renders as **"200 gp"**, not "20 pp". Under `baseCurrency: "pp"` the same value renders as "20 pp".
    3. **Sub-cp results round to nearest cp** — `1 cp × 0.1 = 0.1 cp` becomes `1 cp` (cp is the floor; ties go up). The rounding happens once at the modifier boundary, not on every display.
  - **Named presets (UI shortcuts; the canonical D&D standards).** Party Settings (§5.15) offers a one-click chooser that sets both knobs at once. "Custom" lets the DM pick `priceModifier` and `baseCurrency` independently for homebrew economies that don't fit a named ladder. The presets are pure UI sugar — storage is always the two raw fields.
    | Preset name | `priceModifier` | `baseCurrency` | Intent |
    |---|---|---|---|
    | **Gold standard** *(default)* | `1.0` | `"gp"` | Canonical 2024 PHB economy. |
    | **Silver standard** | `0.1` | `"sp"` | The "1 gp PHB price = 1 sp at the table" variant. Sub-sp items round to cp. |
    | **Copper standard** | `0.01` | `"cp"` | Grim-scarcity economies; everything denominated in copper. |
    | **Electrum standard** | `0.5` *(× 0.1 from gp + × 5 sp→ep)* | `"ep"` | Niche / historical. Most tables won't use this. |
    | **Platinum standard** | `1.0` | `"pp"` | High-magic / late-tier campaigns where pp is the unit of account; PHB prices stay numerically the same but roll up to pp where they divide cleanly. |
    | **Custom** | DM-set float | DM-set enum | Any homebrew economy (`2.0` inflation, `0.25` grim, etc.). |
  - **Homebrew items are stored in the price the DM typed** (real coins, real value) and are **not** scaled by `priceModifier`. They ARE subject to `baseCurrency` canonicalization on display (so the DM doesn't see "1.5 gp" on a homebrew priced at 15 sp). The `ItemDefinition.source` field (already `"PHB" | "DMG" | "homebrew"` in §4) is the discriminator for the modifier; canonicalization is universal.
  - Exposed via the §5.17 "Variant-rules toggle" surface in Party Settings (§5.15). See §12.

### 3.6 Encumbrance (2024 rules)
- Carrying capacity = STR × 15.
- **Encumbered** at > 5×STR (variant rule); **heavily encumbered** at > 10×STR.
- **Encumbrance applies ONLY to the character's Inventory stash.** Storage stashes (chests, vaults, etc.) do not count toward carrying capacity.
- **Containers**: **one level deep** within a stash (item-in-bag, no bag-in-bag). **Bag of Holding** and similar handled as flat-weight exceptions.
- Visual capacity bar on the Inventory screen; warning states.
- Enforcement level configurable per character (see 3.3).

### 3.7 Item Catalog
- **PHB 2024**: weapons, armor, adventuring gear, tools, ammunition.
- **DMG 2024**: full magic-item set by rarity (Common → Artifact).
- **Custom items** module with same schema + `source: "homebrew"`.
- PHB/DMG entries are read-only; **"Duplicate to edit"** clones them into homebrew.
- **Custom-item authorship**:
  - When a party has **only one member** (party-of-one / solo): that user can create custom items freely.
  - When a party has **2+ members**: only the **DM** can create custom items (party-scoped catalog), to prevent griefing and keep the shared catalog curated.
- **Search**: fuzzy across name + description + tags; filter by category/rarity/attunement-required/cost/source.
- "Add to character" / "Add to party stash" actions.

### 3.8 Magic Items & Attunement
- Rarity tags + color coding.
- Attunement requirement flag; prerequisites (class, alignment, species) **displayed as advisory**.
- **Full charge tracking**: current/max, recharge rule (dawn / dusk / long rest / short rest / custom).
- "Long rest" / "Dawn" / "Dusk" buttons batch-recharge applicable items.
- **Identification state**: DM toggles "identified". Unidentified items appear to players as "Unknown Magic Item" with a **DM-set hint**.

### 3.9 Shop & Vendor Module
- **Static catalog** per shop: item list + per-item override price (or shop-wide multiplier).
- Items show base PHB price; shop applies modifier (e.g., 1.2×) or fixed override.
- "Sell to merchant" defaults to **50%** of base.
- **Manual purchase flow**: DM resolves each buy/sell as an explicit transfer (no live shopping session in v1).

### 3.10 Loot Distribution (DM)
- **Per-hoard choice** at distribution time:
  - **Shared pool** — items go to party loot; players claim them freely (no claim cap in v1).
  - **Direct assign** — DM assigns each item to a specific player.

### 3.11 Audit & History
- **Per-item history** (creation, transfers, consumption) — visible to current owner and DM.
- **Party log**: chronological transaction list filtered by session/character/item.
- All mutations write to the log.

### 3.12 Session Tools
- **Lightweight session tags**: DM marks the current session; transactions are tagged with it.
- "Distribute loot" wizard tags items to the active session.
- Per-session filter on history view.

### 3.13 Backups
- **Self-hosted server takes nightly snapshots** to disk; retention policy configurable.
- **User-triggered JSON export** still available client-side for portable backups.
- **JSON import** replaces all current data (with confirm).

### 3.14 Party Banker (Treasurer)
- Available only when the party has **2+ members**. In a party-of-one the role doesn't exist (the DM handles distribution directly).
- The **DM appoints** zero or one Banker from among the party's players; the DM can revoke or reassign the role at any time.
- **When a Banker is appointed**, the Party Stash and Recovered Loot become **Banker-mediated pools** — players can no longer self-claim from them; all distributions go through the Banker.
- **When no Banker is appointed**, players self-claim freely from both pools (the Party Stash and Recovered Loot behave as communal piles).
- The Banker manages **both the Party Stash and the Recovered Loot stash** — currency and items — and can perform the full distribution toolkit:
  - **Split currency evenly** across all current characters.
  - **Give** specific amounts of currency to a named player.
  - **Take** currency into their own character's purse (Inventory or any Storage they own).
  - **Distribute items** to specific players.
  - Same operations apply to Recovered Loot (acts as a secondary pool the Banker dispatches from).
- **DM authority while a Banker is active**:
  - DM **can still**: add currency/items to the Party Stash or Recovered Loot (e.g., loot acquired in play, character departures); remove currency/items for **gameplay reasons** (e.g., a magical drain, theft, an NPC takes a cut).
  - DM **cannot** while Banker is active: distribute Party Stash or Recovered Loot currency/items to a specific player's character — that's the Banker's job. The DM would have to revoke the Banker role to resume direct distribution.
- **Auditing**: every Banker action is logged with `actorUserId` plus `actorRole: "banker"` and is **visible to all party members** in the party log.
- **Role transitions**: if the Banker leaves or is kicked, the role **auto-clears**; the DM must reappoint someone to restore Banker-only operations (otherwise direct DM distribution resumes).
- **Self-appointment**: the DM cannot appoint themselves as Banker; the role is for delegating to a player.

### 3.15 User-Facing Terminology
- **Inventory** — a character's carried stash (encumbrance applies).
- **Storage** — any other character-owned stash (chest, vault, wagon; no encumbrance).
- **Party Stash** — shared treasure & gear pool for the whole party.
- **Recovered Loot** — items/currency left behind when a player leaves or is kicked; visible to all members.
- **Catalog** — global searchable list of item definitions (PHB, DMG, homebrew).

---

## 4. Data Model (Sketch)

> Stack-agnostic; tables/collections. Deployment mode (local vs synced) is **not** a data attribute — it's an environment/install choice. **Every user is always in at least one Party**; "solo" is just a party-of-one.

### `User`
- id, discordId, displayName, avatarUrl, createdAt
- Single global identity; party-specific role lives on `PartyMembership`.

### `Party`
- id, name, ownerUserId (the current DM; transferable), inviteCode (rotatable; generated at party creation), createdAt
- recoveredLootStashId — direct FK to the per-party Recovered Loot stash, **auto-created on party creation**.
- **bankerUserId** (nullable) — the currently-appointed Banker. Must reference a user with an active `PartyMembership` (role = `player`) in this party, and **must not equal `ownerUserId`** (DM can't be Banker). Auto-cleared when that membership ends. Only legal when `memberCount >= 2`.
- isSoloShortcut (bool) — marks parties created via "Create solo (party-of-one)". Purely a display hint for the hub; behavior is otherwise identical to any other party.

### `PartyMembership`
- **Primary key**: composite `(userId, partyId, role)` — a single user can hold multiple roles in the same party (e.g., the creator is both `dm` and `player`).
- userId, partyId, role (`dm` | `player`), characterId (nullable; only meaningful when `role === "player"`), joinedAt, leftAt (nullable)
- `characterId` is null only in the **brief window between joining the party and creating a character**. A user without a character cannot hold inventory or claim loot.
- A party creator automatically gets **two** memberships at creation: one with `role="dm"` (no character) and one with `role="player"` (with a character). They can later drop the player membership if they don't want to play, without affecting their DM role.
- The **Banker** role is **not** stored here — it's denormalized as `Party.bankerUserId`.
- `actorRole` on `TransactionLog` is **derived at log-write time** from the actor's role in context: it equals `"banker"` if `Party.bankerUserId === actorUserId`, otherwise the relevant `PartyMembership.role` for the action being taken.

### `Character`
- id, partyId (required — every character lives in a party), ownerUserId, name, species, class, level
- abilityScores.STR
- maxAttunement (default 3; DM-overridable)
- encumbranceRule (`off` | `advisory` | `hard`; DM-overridable when 2+ members)
- inventoryStashId — direct FK to the character's auto-created **Inventory** stash (the carried one).

### `Stash`
- id, scope (`character` | `party` | `recovered-loot`), name, createdAt
- **ownership fields** (mutually exclusive depending on scope; see invariants below)
  - ownerCharacterId — set only when `scope === "character"`
  - partyId — set only when `scope === "party" | "recovered-loot"`
- isCarried (bool, default false) — set true on **exactly one** stash per character: the auto-created Inventory. Encumbrance rules apply only when `isCarried === true`. All other character-scope stashes are Storage.
- **Invariants:**
  - `scope === "character"` ⇒ `ownerCharacterId != null`, `partyId == null`. Exactly one stash per character has `isCarried === true` and is referenced by `Character.inventoryStashId`.
  - `scope === "party"` ⇒ `partyId != null`, `ownerCharacterId == null`, `isCarried === false`.
  - `scope === "recovered-loot"` ⇒ `partyId != null`, `ownerCharacterId == null`, `isCarried === false`, and exactly **one** Recovered Loot stash exists per party (the one referenced by `Party.recoveredLootStashId`).

### `ItemDefinition` (catalog entry — the "type")
- id, name, source (`PHB` | `DMG` | `homebrew`)
- category (weapon, armor, gear, tool, magic, consumable, currency, container)
- weight, cost (in cp, base price), rarity (`common` … `artifact` | null)
- requiresAttunement, attunementPrereq (display string)
- properties (damageDice, damageType, AC, range, …)
- charges (max, rechargeRule), description, tags
- duplicatedFromId (for PHB/DMG duplicates)
- createdBy (userId; for homebrew authorship)
- partyId (nullable; set for party-scoped homebrew entries — null for PHB/DMG)

### `ItemInstance`
- id, definitionId
- **ownerType (`stash` | `shop`)** — all character-held items live in a stash (their Inventory or Storage), so there is no separate "character" owner type.
- ownerId — stash.id or shop.id
- containerInstanceId (nullable — single-level nesting only)
- quantity (auto-stacks on `(definitionId, notes)`)
- equipped, attuned, identified, currentCharges
  - **Only meaningful when the containing stash is `scope=character` AND `isCarried=true` (Inventory).** Items in Storage / Party Stash / Recovered Loot / Shop ignore these flags.
- customName, notes
- conditionOverrides (per-instance tweaks)

### `CurrencyHolding`
- id, **stashId** (every stash holds currency uniformly: Inventory, Storage, Party Stash, Recovered Loot), cp, sp, ep, gp, pp
- One row per stash.

### `Shop` (DM-only)
- id, partyId, name
- priceModifier (multiplier, default 1.0)
- sellToMerchantRate (default 0.5)
- stock: `[{ itemDefinitionId, priceOverride? (cp), quantity }]`
  - **`quantity === -1` means unlimited**; otherwise finite stock that decrements on purchase.

### `Session`
- id, partyId, number, date, notes
- isCurrent (only one per party at a time)

### `TransactionLog`
- id, partyId (required — every action happens in some party, even party-of-one), sessionId (nullable), timestamp, actorUserId, actorRole (`dm` | `player` | `banker` — captures which hat the actor was wearing)
- **type + payload as a discriminated union** (each `type` has a typed `payload` shape):
  - `transfer` → `{ itemInstanceId, quantity, fromStashId, toStashId }`
  - `acquire` → `{ itemInstanceId, quantity, source: "hoard" | "purchase" | "catalog-add" | "custom-create" | "duplicate" }`
  - `consume` → `{ itemInstanceId, quantity }`
  - `purchase` / `sale` → `{ itemInstanceId, quantity, currencyDelta: CurrencyDelta, shopId }`
  - `attune` / `unattune` → `{ itemInstanceId, characterId }`
  - `equip` / `unequip` → `{ itemInstanceId, characterId, slot? }`
  - `identify` → `{ itemInstanceId, previousHint?, newHint? }`
  - `recharge` → `{ itemInstanceId, from, to, trigger: "dawn" | "dusk" | "long-rest" | "short-rest" | "manual" }`
  - `use-charge` → `{ itemInstanceId, amount }`
  - `edit-item-instance` → `{ itemInstanceId, changedFields: ("customName" | "notes" | "identified" | "equipped" | "attuned" | "currentCharges" | "conditionOverrides")[] }` — generic per-instance editor for fields that don't have a dedicated TxType. The full new value lives on the instance itself; only the changed field names are logged (mirrors `edit-homebrew`).
  - `currency-change` → `{ stashId, delta: CurrencyDelta, reason?: "deposit" | "withdraw" | "split-evenly" | "gameplay-drain" | "convert" | "stash-deleted" }` — `"stash-deleted"` is emitted by the `delete-stash` cascade when the deleted stash held non-zero currency (the delta rolls into Recovered Loot's `CurrencyHolding`). Added in M3.
  - `join-party` / `leave-party` → `{ partyId, characterId? }`
  - `dm-transfer` → `{ fromUserId, toUserId }`
  - `kick-player` → `{ kickedUserId }`
  - `appoint-banker` → `{ bankerUserId }`
  - `revoke-banker` → `{ formerBankerUserId, reason: "manual" | "left-party" | "kicked" | "reassigned" }`
  - `create-character` → `{ characterId, name }`
  - `delete-character` → `{ characterId, name, lastSessionId? }`
  - `rename-character` → `{ characterId, oldName, newName }` — dedicated type for the most common character edit; mirrors `rename-stash` / `rename-party`.
  - `edit-character` → `{ characterId, changedFields: ("species" | "class" | "level" | "str" | "maxAttunement" | "encumbranceRule")[] }` — catch-all for the remaining mutable character fields per §3.3 + §8.1.
  - `create-stash` → `{ stashId, scope, name, ownerCharacterId? }`
  - `rename-stash` → `{ stashId, oldName, newName }`
  - `delete-stash` → `{ stashId, name, itemCount, currencyTotalCp, ownerCharacterId? }` — `delete-stash` records the snapshot at deletion time so the audit trail explains where items went (they're moved to Recovered Loot or the owning character's Inventory before deletion; the move is its own `transfer` log entry). `ownerCharacterId` is present iff the deleted stash was character-scope (Storage) — captured so post-delete history views can render the original owner alongside the stash name (added in M3 after the initial cut).
  - `rename-party` → `{ partyId, oldName, newName }`
  - `create-homebrew` → `{ definitionId, name }`
  - `edit-homebrew` → `{ definitionId, changedFields: string[] }`
  - `delete-homebrew` → `{ definitionId, name }`
- Where `CurrencyDelta = { cp?: number; sp?: number; ep?: number; gp?: number; pp?: number }` (signed deltas per denomination).
- The party log UI is a filtered view over this table; **per-item history is queried directly off the log** (no separate `ItemHistory` table).

### `Metadata` (server-side) / `AppState.seedVersion` (client-side)
- Server: a small key/value `Metadata` table tracks `seedVersion` for the canonical PHB/DMG content.
- Client: `AppState.seedVersion` mirrors the value used at last seed; bumping triggers a re-seed (upserts PHB/DMG entries, preserves homebrew).

---

## 5. UI / Screens

### Entry / Identity
1. **Login** — single "Sign in with Discord" button.
2. **Hub (post-login)** — three cards: *Create party*, *Join party (paste code)*, *Create solo (party-of-one)*. Below: list of the user's existing parties (solo parties marked with a small "solo" badge).

### Player-facing
3. **Character Sheet** — header with character details (name, species, class, level, STR). Tabs:
   - **Inventory** — carried stash; equipped slots, attunement (X/max), capacity bar, container view (one level), item list with filters.
   - **Storage** — list of additional named stashes; click into one to see/edit its items + currency.
4. **Item Detail** — full description, charges, **per-item history**.
5. **Party Stash** — shared list; deposit/take actions. When viewed by the **Banker**, shows additional "Distribute" controls (split-evenly, give-to-player, give-items-to-player). When viewed by the **DM with Banker active**, distribute-to-player controls are hidden; "add/remove for gameplay" controls remain.
6. **Recovered Loot** — visible to all party members; same Banker/DM control split as Party Stash.
7. **Transfer Modal** — pick item(s), quantity, source stash, target stash.
8. **History/Log** — filterable timeline (session/character/item/type/actorRole).

### DM-facing (additional)
9. **DM Dashboard** — all characters at a glance, party stash, recovered loot, gold totals.
10. **Loot Distribution Wizard** — per-hoard: shared pool OR direct assign.
11. **Hoard Generator** — DMG 2024 tables, CR/level band; output feeds the wizard.
12. **Shop Manager** — build/edit shop catalogs and modifiers; resolve purchases manually.
13. **Identification Panel** — toggle identified, set hint text.
14. **Custom Item Editor** — full form matching `ItemDefinition`.
15. **Party Settings** — invite code (regenerate/revoke), remove player, attunement-cap overrides, encumbrance-rule overrides, **appoint / revoke Banker**, transfer DM role.

### Shared
16. **Item Catalog** — global search & filter; "Add to…" picker.
17. **Settings** — theme (light/dark toggle, system default), variant rules, export/import, account/logout.

### Form factor
- **Desktop-first** (the primary device, especially for DM).
- **Player views responsive on mobile** — the character sheet, party stash, recovered loot, transfer modal, item detail are all mobile-usable.
- DM tools (dashboard, loot wizard, shop manager) are **desktop-only** in v1.

---

## 6. Rules Logic Modules

Pure / deterministic / unit-testable:

- `capacity.ts` — STR-based limits; encumbrance state; enforcement-level handling.
- `attunement.ts` — slot tracking with DM-overridable cap; prereq display (advisory).
- `currency.ts` — conversion, GP-equivalent, "split evenly", normalize.
- `charges.ts` — recharge rules (dawn/dusk/long/short rest/custom); batch recharge.
- `weight.ts` — single-level container weight with Bag-of-Holding-style exceptions.
- `hoard.ts` — DMG 2024 treasure tables.
- `validation.ts` — equip slot conflicts (2H + shield, etc.).
- `pricing.ts` — base price × shop modifier; "sell to merchant" at default 0.5×.
- `search.ts` — fuzzy multi-field scoring across catalog.

---

## 7. Content Seeding

- **Seed scripts**:
  - `seed/phb-2024.json` — PHB 2024 mundane items (always seeded).
  - `seed/dmg-2024.json` — DMG 2024 magic items (always seeded).
- Versioned with `seedVersion` for reproducible updates; re-seed upserts PHB/DMG entries while preserving homebrew.
- Homebrew lives in the same `ItemDefinition` table with `source: "homebrew"` and `createdBy`.

---

## 8. Permissions Model

> **Party-of-one (1 member).** The sole member is both DM and player. Custom-item creation, character editing, etc., follow permissive defaults. **No Banker role exists** at this size.

> **Party with 2+ members.** Full party rules apply: DM read-all + write-via-explicit-actions; DM-only custom items; optional Banker.

> **DM principle.** The DM never silently edits a player's character. Every cross-character mutation goes through an **explicit action** (give, take, identify, set-cap, force-recharge) that is recorded in the per-item history and party log.

> **Display invariant — identification.** Players always see unidentified items as "Unknown Magic Item" plus the DM-set hint. The real name only appears once the DM toggles `identified`. This is a display rule, not a permission row.

> **Banker.** A player optionally designated by the DM (zero or one per party of 2+; the DM cannot self-appoint). The Banker is *also* a Player — they retain every Player permission and gain Banker-only rights over **both the Party Stash and the Recovered Loot stash**. When a Banker is active, the DM may still **add or remove** currency/items in either pool for gameplay reasons, but cannot **distribute** to specific players. The DM can revoke the Banker at any time to resume direct distribution.

### 8.1 Party-Context Permission Matrix (2+ members)

| Action | Player (self) | Player (others in party) | Banker (Party Stash + Recovered Loot) | DM (in own party) |
|---|---|---|---|---|
| View own Inventory & Storage | ✅ | — | ✅ | ✅ |
| View other players' Inventory & Storage | — | ❌ | ❌ | ✅ (read-all) |
| Edit own inventory (move, consume, equip/unequip, attune/unattune within cap) | ✅ | ❌ | ✅ | — |
| Create/rename/delete own character's Storage stashes | ✅ | ❌ | ✅ | ✅ (via explicit action) |
| Edit own item instances (notes, customName, quantity adjust) | ✅ | ❌ | ✅ | ✅ (via explicit action) |
| Edit `ItemDefinition` (homebrew) | ❌ | ❌ | ❌ | ✅ |
| Edit other players' inventory (silent) | — | ❌ | ❌ | ❌ |
| Edit other players' inventory via explicit action (give / take / identify / force-recharge / set-cap) | — | ❌ | ❌ | ✅ |
| Edit own character name / species / class / level | ✅ | ❌ | ✅ | ✅ (any character, via explicit action) |
| Edit own character STR | ✅ (logged) | ❌ | ✅ | ✅ (any character, via explicit action) |
| Edit any character max attunement | ❌ | ❌ | ❌ | ✅ |
| Edit any character encumbrance rule | ❌ | ❌ | ❌ | ✅ |
| Transfer item to another player directly (own → other) | ✅ | (receiver — auto-accepts) | ✅ | ✅ |
| View Party Stash & Recovered Loot | ✅ | ✅ | ✅ | ✅ |
| **Claim items/currency from Party Stash** | ✅ (only when no Banker active) | ✅ (only when no Banker active) | ✅ (always) | ✅ (only when no Banker active) |
| **Claim items/currency from Recovered Loot** | ✅ (only when no Banker active) | ✅ (only when no Banker active) | ✅ (always) | ✅ (only when no Banker active) |
| **Add currency/items to Party Stash or Recovered Loot** | ✅ (deposit own) | — | ✅ | ✅ |
| **Remove currency/items from Party Stash or Recovered Loot for gameplay reasons (drain, NPC tax, etc.)** | ❌ | — | ❌ | ✅ |
| **Distribute Party Stash / Recovered Loot currency to a specific player (give X gp to Player Y)** | ❌ | — | ✅ | ❌ (blocked while Banker active; revoke first) |
| **Split Party Stash currency evenly across all characters** | ❌ | — | ✅ | ❌ (blocked while Banker active) |
| **Take Party Stash / Recovered Loot currency into own character's purse** | ❌ | — | ✅ | — |
| **Distribute Party Stash / Recovered Loot items to a specific player** | ❌ | — | ✅ | ❌ (blocked while Banker active) |
| Use charge on own attuned item | ✅ | ❌ | ✅ | ✅ (any item via force-action) |
| Recharge own item (manual trigger when rules allow) | ✅ | ❌ | ✅ | ✅ (force-recharge any item) |
| Create custom item (homebrew) | ❌ | ❌ | ❌ | ✅ |
| Run hoard generator | ❌ | — | ❌ | ✅ |
| Drop loot into shared pool | ❌ | — | ❌ | ✅ |
| Assign loot directly to a player | ❌ | — | ❌ | ✅ |
| Identify magic item (toggle identified) | ❌ | — | ❌ | ✅ |
| Set / edit unidentified-item hint | ❌ | — | ❌ | ✅ |
| Manage shops (create, edit catalog, set modifier) | ❌ | — | ❌ | ✅ |
| Resolve a buy/sell transaction | ❌ | — | ❌ | ✅ |
| Regenerate / revoke invite code | ❌ | — | ❌ | ✅ |
| Kick a player | ❌ | — | ❌ | ✅ |
| Leave the party (self) | ✅ | — | ✅ (Banker role auto-clears on leave) | ✅ (DM leaving auto-triggers DM transfer; if no other members, party is archived) |
| Transfer DM role to another member | ❌ | — | ❌ | ✅ (immediate; no acceptance required) |
| **Appoint / revoke Banker** | ❌ | — | ❌ | ✅ |
| Set / advance current session | ❌ | — | ❌ | ✅ |

### 8.2 Party-of-One Permissive Defaults

In a party with only the DM (no joined players), the sole member's effective rights are the **union** of DM and Player rights, plus:
- **Create custom items freely** (no DM-only restriction).
- **Banker role does not exist** — distribution to "self" is moot.
- All "blocked while Banker active" restrictions on the DM are inert.

The moment a second user joins (joinedAt set on a non-DM `PartyMembership`), the full 8.1 matrix takes effect.

### 8.3 Leaving / Kicking Behavior

When a player **leaves** their own party (self-service) **or is kicked** by the DM:
- The player's character row is detached from the party (soft-deleted via `PartyMembership.leftAt`).
- All items owned by that character are moved to the party's **recovered-loot stash**.
- All currency held by that character is added to the recovered-loot stash's currency holding.
- A `leave-party` or `kick-player` entry is written to the transaction log.
- **If the departing player was the Banker**, `Party.bankerUserId` is auto-cleared and a `revoke-banker` log entry (`reason: "left-party"` or `"kicked"`) is written. The DM may appoint a new Banker at any time; until then, direct DM distribution to specific players resumes.

When the **DM** leaves:
- If other members exist, the DM must first transfer the DM role; the leave then proceeds as above.
- If no other members exist, the party is archived (no destructive delete; data preserved for the DM's records).

---

## 9. Architecture

### Local mode (browser-only)

```
[ Browser SPA ] ── reads/writes ──► [ localStorage / IndexedDB ]
       │
       └── bundled seed (PHB + DMG)
```

### Self-hosted mode (Linux server)

```
[ Browser SPA ] ──HTTPS──► [ API Server ] ──► [ DB + nightly snapshots ]
       ▲ ▼                       │
       │ websocket               ├── Discord OAuth (token exchange)
       │                         └── Rules Engine (pure)
       └─── live party sync ◄────┘
```

- **Server-authoritative**: server is the source of truth; clients push/pull, server arbitrates.
- **Realtime via websocket**: party members see updates within seconds.
- **Auth**: Discord OAuth2 (authorization code flow with PKCE); minimal scope `identify`; session cookies after token exchange.
- **Rules engine** is shared code (pure) — runs server-side for authoritative validation, client-side for optimistic UI.
- **Offline behavior**:
  - **Multi-member parties**: require a connection; show offline banner; block writes; auto-resume on reconnect.
  - **Party-of-one (solo)**: works offline indefinitely; uses local cache; syncs to server on next connection.
- **Backups**: nightly snapshots to disk (default retention: 30 days, configurable); user JSON export remains available.

---

## 10. Milestones

| Milestone | Deliverable |
|---|---|
| **M0 — MVP (already scoped)** | Local-only solo tracker: stashes, currency, PHB mundane catalog, custom items, JSON export. See `MVP.md`. |
| **M1 — Characters & encumbrance** | Character entity (inventory-only data); equip; encumbrance with toggle (off/advisory/hard); single-level containers + Bag of Holding. |
| **M2 — Magic items** | DMG 2024 seed; attunement w/ warnings + DM cap override; charges with batch recharge buttons. |
| **M3 — Backend skeleton** | Self-hosted server, Discord OAuth, user model, sync of solo data; nightly snapshots. |
| **M4 — Multi-member parties** | Invite codes, multi-user joining, Party Stash, Recovered Loot, Banker appointment + distribution toolkit, DM/Player role split when 2+ members. |
| **M5 — Live sync & history** | Websocket sync; per-item history; party log with session-tag filter; offline banner in party mode. |
| **M6 — DM tools** | Loot distribution wizard (per-hoard mode), hoard generator, identification flow with hints, shop manager (static + modifiers). |
| **M7 — Polish** | Light/dark theme, responsive player views (mobile), fuzzy multi-field search, accessibility pass. |

---

## 11. Open Questions

- **Snapshot retention** — default is 30 days; should this be exposed in admin settings, or hard-coded?
- **Discord outage fallback** — if Discord OAuth is unreachable, should existing sessions remain valid for N days?
- **Invite code lifetime** — single-use vs reusable? Time-bounded? (Suggest: reusable until DM rotates, no time limit.)
- **Recovered-loot pruning** — does the pile grow forever, or auto-expire stale items?
- **History detail level** — store only owner transitions, or every quantity/charge/note edit too? (Suggest: ownership transitions only on the item history; everything else in the party log.)
- **Default Storage stashes on character creation** — auto-create a single "Storage" stash, or start with zero (user adds them on demand)?
- **DM-as-player on creation** — when creating a party, should the creator be prompted "Do you also play a character in this party?" with explicit yes/no, or always auto-add a player membership that can be deleted later?

---

## 12. Future / Stretch

- **Live shopping session** — promote shop module from static to live (players browse + buy in real time).
- **Crafting tracker** (downtime, components).
- **Wear-and-tear / item conditions** (homebrew-friendly).
- **Item wishlist** per character (DM hints).
- **Print-friendly inventory sheet** (PDF).
- **VTT integration** (Foundry/Roll20 character link).
- **Public party directory** (opt-in) for finding open campaigns.
- **Light character sheet expansion** — AC, HP, proficiencies for fuller display.
- **Bulk currency entry** (see §3.5). *Scheduled to R7 alongside the bulk multi-select cluster (see roadmap.md). Inline signed-integer / absolute-target syntax on each denomination cell; same `currency-change` schema. The full spec stays in §3.5; this row is kept as a backlink for context.*
- **Per-party economy controls** (see §3.5). *Scheduled to R6 alongside `pricing.ts` activation, `Shop`, and `purchase`/`sale` (see roadmap.md). Two knobs (`Party.priceModifier`, `Party.baseCurrency`) plus a Settings preset chooser (Gold / Silver / Copper / Electrum / Platinum / Custom). The full spec stays in §3.5; this row is kept as a backlink for context.*
